# housing_market_ml

source venv/bin/activate


Flow:

( * fetch data )
* preprocess (data)

( * select / produce features )

( * tune hyperparameters )

* train model

( * cross validate )

* predict
